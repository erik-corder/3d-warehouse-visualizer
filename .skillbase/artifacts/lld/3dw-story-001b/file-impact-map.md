# File Impact Map: 3DW-STORY-001B

Companion to `low-level-design.md`. This is a map for future implementation; no implementation files are created by this LLD.

## Expected files to create

**Frontend (10):**
- `frontend/package.json`
- `frontend/tsconfig.json`
- `frontend/electron.vite.config.ts`
- `frontend/electron/main.ts`
- `frontend/src/renderer/index.html`
- `frontend/src/renderer/main.tsx`
- `frontend/src/renderer/App.tsx`
- `frontend/.eslintrc.cjs`
- `frontend/.prettierrc`
- `frontend/.husky/pre-commit`

**Frontend tests (1):**
- `frontend/src/renderer/App.test.tsx`

**Total new files: 11**

## Expected files to modify

None. This story creates only new, isolated files under `frontend/`.

**Note, learned from `3DW-STORY-001A`'s implementation:** `dotnet new`-equivalent scaffolding for `.NET` produced two structurally-necessary companion files not itemized in that story's original file-impact-map (`Properties/launchSettings.json`, the test project's `.csproj`), which required a separate scope-change approval. The frontend equivalents most likely to recur here are a lockfile (`package-lock.json` or equivalent, generated by the package manager) and possibly a `.gitignore` addition for `frontend/node_modules` if the root `.gitignore`'s generic `node_modules/` pattern does not already cover a nested path (it should, but flagged for verification during implementation). **Pre-flagged here, in the LLD itself, specifically to avoid a repeat scope-change approval cycle** — if a lockfile is generated, treat it as within this story's approved scope (a standard, zero-logic package-manager artifact), not a deviation requiring separate sign-off.

## Files explicitly not to touch

`backend/**` (out of scope — already implemented by `3DW-STORY-001A`), `docker-compose.yml` (out of scope — `3DW-STORY-001D`), `frontend/src/renderer/design-system/**` (out of scope — `3DW-STORY-001C`, does not exist yet), `.env.example`, `README.md` (owned by `3DW-STORY-001D`), `.skillbase/**`, `docs/adr/**`, `CLAUDE.md`, `.claude/**`.

## Expected backend impact

None — backend already exists and is untouched by this story.

## Expected frontend impact

New, isolated `frontend/` directory. No existing frontend code exists to modify (first frontend story).

## Expected database impact

None.

## Migration expectation

**No.**

## Docker expectation

**No** — Docker Compose is `3DW-STORY-001D`'s scope. This story is verified by running the Electron app natively against the already-running `3DW-STORY-001A` backend (`dotnet run`).

## Test files expected

1: `App.test.tsx` (component test, three UI states via mocked `fetch`). Included in the total above.

## PR size estimate

**Total changed files: 11 (10 create + 1 test).**

**Size: M** (11-15 files, per the skill's guardrail table). Within guardrails — not "Too large."

## Split recommendation

None needed. 11 files is within the M guardrail as a single PR; no further split is warranted for this child story.
