/**
 * Shadow (elevation) tokens: one subtle level, sufficient for a small
 * atom like `StatusBadge`. Not a full elevation system.
 */
export const shadows = {
  low: '0 1px 2px rgba(0, 0, 0, 0.08)',
} as const;
