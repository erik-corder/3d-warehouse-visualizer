/**
 * Spacing tokens: a small numeric scale, in pixels. Kept minimal per
 * 3DW-STORY-001C's scope - extend only when a later UI story actually
 * needs a value not covered here.
 */
export const spacing = {
  xs: '4px',
  sm: '8px',
  md: '12px',
  lg: '16px',
  xl: '24px',
} as const;
